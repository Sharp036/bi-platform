spring:
  profiles:
    active: test
  datasource:
    url: jdbc:tc:postgresql:16:///datalens_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

datalens:
  jwt:
    secret: test_secret_at_least_32_characters_long_for_jwt_signing
    access-token-expiration: 3600000
    refresh-token-expiration: 604800000
  admin:
    username: admin
    password: admin
    email: admin@test.local
