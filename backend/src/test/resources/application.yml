spring:
  profiles:
    active: test
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:tc:postgresql:16:///datorio_test}
    username: ${DB_USER:test}
    password: ${DB_PASSWORD:test}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

datorio:
  jwt:
    secret: test_secret_at_least_32_characters_long_for_jwt_signing
    access-token-expiration: 3600000
    refresh-token-expiration: 604800000
  admin:
    username: admin
    password: admin
    email: admin@test.local
